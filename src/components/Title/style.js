import styled from "styled-components";
import { textColor } from "../../globalStyle";

export const Container = styled.div`
  padding: 0 20px;
  color: ${textColor};
  font-size: 30px;
  font-family: "Nunito", Arial, Helvetica, sans-serif;
  box-shadow: 0 5px 7px #0001;
`;
